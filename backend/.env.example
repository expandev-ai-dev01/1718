# Server Configuration
NODE_ENV=development
PORT=3001
API_VERSION=v1

# Database Configuration (SQL Server)
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=your_strong_password
DB_NAME=lovecakes_db
DB_ENCRYPT=true

# CORS Configuration (Frontend URLs)
# Development: multiple localhost ports supported by default in config
# Production: set allowed origins, comma-separated
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Security
# Add any security-related keys here in the future (e.g., JWT_SECRET)
